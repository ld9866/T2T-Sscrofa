# Repeat Analysis

# Generate segmental duplication (SD) annotations using BISER
biser -o tabasco.softmask.renamed.SD -t 8 tabasco.softmask.fa --gc-heap 10G

# 
barrnap --kingdom euk --threads 128 --outseq tabasco.pig.rDNA.fa tabasco.pig.fa > tabasco.pig.rDNA.gff
